{
  "name": "@cso-voting/backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env test",
    "deploy": "pnpm exec tsx scripts/validate-frontend-build-env.ts && wrangler deploy --minify",
    "deploy:staging": "pnpm exec tsx scripts/validate-frontend-build-env.ts && wrangler deploy --minify --env staging",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint . && oxfmt --check .",
    "lint:fix": "oxlint . --fix && oxfmt .",
    "test": "vitest",
    "db:migrate": "drizzle-kit migrate",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:migrate:offline": "TURSO_DATABASE_URL=http://127.0.0.1:8080 TURSO_AUTH_TOKEN=offline-local-token NODE_ENV=development OFFLINE_DEV=true tsx scripts/offline-migrate.ts",
    "db:seed-admin:offline": "TURSO_DATABASE_URL=http://127.0.0.1:8080 TURSO_AUTH_TOKEN=offline-local-token NODE_ENV=development OFFLINE_DEV=true tsx scripts/seed-admin.ts",
    "db:seed-superadmin:offline": "TURSO_DATABASE_URL=http://127.0.0.1:8080 TURSO_AUTH_TOKEN=offline-local-token NODE_ENV=development OFFLINE_DEV=true tsx scripts/seed-superadmin.ts",
    "db:seed-voter:offline": "TURSO_DATABASE_URL=http://127.0.0.1:8080 TURSO_AUTH_TOKEN=offline-local-token NODE_ENV=development OFFLINE_DEV=true tsx scripts/seed-voter.ts",
    "e2e:worker:offline": "pnpm db:migrate:offline && wrangler dev --env offline --port 8787",
    "db:import-students": "tsx scripts/import-students.ts",
    "db:seed-admin": "tsx scripts/seed-admin.ts",
    "db:seed-superadmin": "tsx scripts/seed-superadmin.ts",
    "db:seed-voter": "tsx scripts/seed-voter.ts",
    "db:reset-password": "tsx scripts/reset-password.ts",
    "db:anonymize-votes": "tsx scripts/anonymize-votes.ts",
    "clean": "rm -rf dist .wrangler"
  },
  "dependencies": {
    "@cso-voting/student-csv-parser": "workspace:*",
    "@hono/zod-openapi": "^0.19.10",
    "@libsql/client": "^0.15.15",
    "@scalar/hono-api-reference": "^0.9.34",
    "backblaze-b2": "^1.7.1",
    "bad-words": "^4.0.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.27",
    "hono-pino": "^0.8.0",
    "pino": "^9.14.0",
    "pino-pretty": "^13.1.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/backblaze-b2": "^1.5.6",
    "@types/node": "^20.19.27",
    "@vitest/coverage-v8": "^3.2.4",
    "dotenv": "^16.6.1",
    "dotenv-expand": "^12.0.3",
    "drizzle-kit": "^0.31.8",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0",
    "pdf-parse": "^2.4.5",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite-tsconfig-paths": "^5.1.4",
    "vitest": "^3.2.4",
    "wrangler": "^4.106.0"
  }
}
