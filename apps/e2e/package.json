{
  "name": "@cso-voting/e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "export OFFLINE_DEV=true TURSO_DATABASE_URL=http://127.0.0.1:8080 TURSO_AUTH_TOKEN=offline-local-token PUBLIC_API_BASE_URL= PUBLIC_OFFLINE_DEV=true PUBLIC_SENTRY_DSN= && pnpm --dir ../frontend build && playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@libsql/client": "^0.15.15",
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.19.27",
    "dotenv": "^16.4.7"
  }
}
